package web

templ Table(teams []string, services []string, state bool) {
    <!DOCTYPE html>
    <html>
    <head>
    <style>
    table, th, td, tr {
        border:1px solid black;
        font-size: 40px;
    }
    </style>
    
    <table>
        <tr>
            <th>{ }</th>
            for _, service := range services {
                <th>{ service }</th>
            }
        </tr>
        for _, team := range teams {
            <tr>
                <td>{ team }</td>
                for i := 0; i < len(services); i++ {
                //TODO - get templ to display images
                    <td> <img src="/web/assets/up.png" alt="Green up arrow"/> </td>
                }
            </tr>
        }   
    </table>
    </head>
    </html>
}